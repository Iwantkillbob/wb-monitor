# WB Monitor 完整运行手册（v1.1.1）

> 单一可信文档：覆盖 **日常使用** → **打包为 exe** → **发布到 GitHub Release** 全流程，
> 并收录本机所有已知环境坑（Windows Defender 卡死、代理/git 推送）。
> 维护者：jarves ｜ 仓库：`Iwantkillbob/wb-monitor`（public，默认分支 `master`）

---

## 0. 关键事实速查

| 项 | 值 |
|---|---|
| 版本 | `1.1.1`（代码已就绪并推送 `master`，最新 commit `e9c6f9e`；**Release 尚未发布**，GitHub 上当前公开版仍为 v1.1.0） |
| 技术栈 | Electron 32 ＋ sql.js(WASM) ＋ 零原生依赖 |
| 形态 | 悬浮球（56px，纯悬浮不占任务栏）＋ 系统托盘（青色图标）双入口，24h 常驻 |
| 数据源 | tokenSource(WB 本地 JSONL) ／ costSource(`~/.workbuddy/workbuddy.db`) ／ ccSource(`~/.claude/projects` JSONL) ／ harness-source(本机 AI harness 自动发现) |
| 源码目录 | `D:\workbody\2026-07-31-11-14-14\wb-monitor` |
| 打包输出目录 | `D:\workbody\2026-07-31-11-14-14\wb-monitor-output`（即 `build.bat` 里的 `../wb-monitor-output`） |
| Release 页 | `https://github.com/Iwantkillbob/wb-monitor/releases` |

**当前状态一句话**：运行时优化已完成并推送（`e9c6f9e`），托盘＋悬浮球都在、`start.bat` 不再误杀 WorkBuddy；
唯一卡点是 v1.1.1 的 exe 打包被 **Windows Defender 实时防护** 卡死，需你在管理员 PowerShell 加一次排除项（见 §4.1）即可解。

---

## 1. 日常使用（推荐：免打包，从源码跑）

> 为什么推荐免打包：打包会把 186MB Electron 二进制＋资源解压到磁盘，触发 Defender 实时扫描锁死（详见 §4.1）。
> 而**源码运行全程异步、零提取、零 Defender 风暴**，托盘＋悬浮球立即可用，专为 24h 常驻设计。

### 1.1 启动
二选一（效果相同）：
- **Windows 直接双击** `D:\workbody\2026-07-31-11-14-14\wb-monitor\start.bat`
- 或命令行：在该目录执行 `npm run start:clean`

### 1.2 界面与操作
| 入口 | 行为 |
|---|---|
| **悬浮球** | 56px 圆形浮窗，显示用量；点击展开/收起面板；`skipTaskbar:true` 不占任务栏 |
| **系统托盘** | 通知区域青色圆图标（内联 base64，打包后也能显示，无需额外文件）；**右键**菜单＝[显示窗口 / 退出]；**左键单击**＝切换窗口显隐 |
| **鼠标穿透** | 快捷键 `Ctrl+Shift+M` 进入"只看不动"模式 |
| **重复启动** | 单实例锁；再点 `start.bat` 不会开第二个，而是聚焦已有窗口 |

### 1.3 关闭旧实例（已修的 footgun）
原 `start.bat` 用 `taskkill /im electron.exe /f` 会杀掉**全机所有 electron 进程（含 WorkBuddy 桌面端）**。
现已改为仅按命令行匹配 `*wb-monitor*` 杀自身进程。**日常重启直接双击 `start.bat` 即可，不会误伤 WorkBuddy。**

### 1.4 首次加载
窗口弹出即成功。首次需几秒扫描 JSONL（可能数千文件），之后有 mtime 缓存，几乎零开销。

---

## 2. 内部机制（优化后的刷新逻辑，commit `e9c6f9e`）

优化维度＝运行时性能/资源占用。结论：**异步、非阻塞、缓存命中为主，适合 24h 常驻。**

| 机制 | 实现 | 效果 |
|---|---|---|
| 刷新节流 | `CONFIG.refresh.tokenMs` 1500ms → **5000ms** | 热路径从"每周期 ~6 次全量目录扫描＋2 次同步递归 walk"降为缓存命中的轻量合并 |
| 目录列表缓存 | `tokenSource`/`ccSource` 的 `listAllSessionFiles` 加 **12s TTL**，返回 `[{path, mtimeMs}]` | 跳过逐文件 `stat`；`readSessionFile` 复用已知 mtime 避免重复 stat；文件内容按 mtime 缓存命中 |
| cc-source 同步→异步 | 原 `fs.statSync` 递归 walk（阻塞主进程）→ `fsp.stat` 异步 | 主进程不再被目录遍历卡住 |
| harness 扫描异步＋缓存 | `_scan` 改 `fsp.stat`；`detectHarnesses` 加 **60s TTL** | 60s 内零重复扫描 |
| ¥/token 费率 | 原每轮全量重算 → `getCachedModelRate` **每 30s** 仅全扫一次 | 去掉每轮最重的 `fetchRecentCallsAsync(999999)` |
| costSource | 已具备 mtime 缓存＋串行锁（防 wasm 堆崩溃），本次未动 | 稳态 `totalCost≈1538`、`dbCalls≈27` 级 |

冒烟验证（本机真实数据）：costSource 正常、harness 6 个全识别、tokenSource 扫到 53 个 WB jsonl 且列表缓存命中 0ms。
（token/cc 聚合为 `null` 仅因本机暂无可解析的 rawUsage 原始记录，属行为保持、非回归。）

---

## 3. 配置（`config.json`，改后重启生效）

```jsonc
{
  "refresh": {
    "networkMs": 3000,   // 网速刷新（ms），代码兜底 ≥2000
    "tokenMs": 5000      // Token 刷新（ms），代码兜底 ≥5000
  },
  "window": {
    "ballSize": 56,
    "panelSize": { "w": 320, "h": 520 }
  }
}
```

---

## 4. 打包为 exe（v1.1.1）

### 4.1 前置条件（必须）：解除 Windows Defender 卡死

**现象/根因**：electron-builder 打包时把 186MB Electron 二进制＋资源写入输出目录，Defender 实时扫描对其持锁不放，
导致 `rename(win-unpacked.tmp → win-unpacked)` 挂十几分钟，随后 `editWindowsResources` 写 exe 时报 `UNKNOWN: unknown error` 退出。
**这不是 app 运行时的问题，是打包过程的问题。**

**解决（在「管理员」PowerShell 中运行，只需一次）**：

```powershell
# 排除项目目录（node_modules / 构建缓存都在这）
Add-MpPreference -ExclusionPath "D:\workbody\2026-07-31-11-14-14\wb-monitor"
# 排除打包输出目录（build.bat 的真实输出，Defender 主要锁这里）
Add-MpPreference -ExclusionPath "D:\workbody\2026-07-31-11-14-14\wb-monitor-output"
# 若你之前从会话目录手动跑过构建，也一并排除那个残留输出：
Add-MpPreference -ExclusionPath "D:\workbody\2026-08-12-10-44-38\wb-monitor-output"
```

验证已生效：
```powershell
Get-MpPreference | Select-Object -ExpandProperty ExclusionPath
```
（沙箱里的 PowerShell 非提权，`Add-MpPreference` 会报"权限不足"——**必须在本机点开始→搜 PowerShell→右键"以管理员身份运行"**。）

备选方案（临时关实时防护，记得开回）：
```powershell
Set-MpPreference -DisableRealtimeMonitoring $true
# 打包完成后务必开回：
Set-MpPreference -DisableRealtimeMonitoring $false
```

> 注：`build.bat` 第 3 步自带 `rd /s /q ..\wb-monitor-output` 会清空旧输出，所以上次失败残留的
> `win-unpacked` / `win-unpacked.tmp` / `win-unpacked.tmp.lock` 会在下次构建被自动清掉——前提是 Defender 已排除，否则清理/重建会再次卡死。

### 4.2 执行打包

双击 `D:\workbody\2026-07-31-11-14-14\wb-monitor\build.bat`，
或在项目目录执行等价命令：

```bat
npx electron-builder --win --config.directories.output=../wb-monitor-output
```

- 产物落在 `D:\workbody\2026-07-31-11-14-14\wb-monitor-output\`：
  - `WB-Monitor-1.1.1-Setup.exe` —— NSIS 安装版（含桌面/开始菜单快捷方式）
  - `WB-Monitor-1.1.1-Portable.exe` —— 便携版（单文件，拷走即用）★ 发布用这个
- 正常耗时 1–3 分钟（含下载 Electron 运行时）。`build.bat` 失败时会把错误写进 `build-log.txt` 并打印末 15 行。
- **打包后的 exe 同样带托盘＋悬浮球**（图标内联 base64，无需外部文件），别人电脑下载即可用。

### 4.3 本机代理 / git 推送坑（仅发布阶段用到）

本机 `HTTPS_PROXY=127.0.0.1:7897` 是共享出口 IP，GitHub API 会被限流(EOF)；且 `git config` 写死了 proxy 指向它。

| 操作 | 正确命令 |
|---|---|
| gh API（限流时用直连） | `env -u HTTPS_PROXY -u HTTP_PROXY gh ...` |
| git push（绕过写死的 proxy） | `git -c http.proxy= -c https.proxy= push origin <branch/tag>` |

---

## 5. 发布到 GitHub Release

前提：`gh` 已登录 `Iwantkillbob`；仓库 public；默认分支 `master`（当前 `e9c6f9e` 已在 `master` 上）。

### 5.1 打 tag 并推送
```bat
cd /d D:\workbody\2026-07-31-11-14-14\wb-monitor
git tag v1.1.1
git -c http.proxy= -c https.proxy= push origin v1.1.1
```

### 5.2 创建 Release 并上传便携版
> gh 命令走直连（去掉代理）以避免限流；一行内写完，勿换行。

```bat
env -u HTTPS_PROXY -u HTTP_PROXY gh release create v1.1.1 --repo Iwantkillbob/wb-monitor --title "WB Monitor v1.1.1" --notes "运行时资源占用优化（24h 常驻）；系统托盘+悬浮球双入口" "..\wb-monitor-output\WB-Monitor-1.1.1-Portable.exe"
```

（若想顺带发布安装版，把 Setup.exe 也加到命令末尾，用空格分隔。）

### 5.3 覆盖策略：删掉旧 v1.1.0
```bat
env -u HTTPS_PROXY -u HTTP_PROXY gh release delete v1.1.0 --repo Iwantkillbob/wb-monitor -y
```

### 5.4 下载链接
发布后公开下载地址格式（`WB Monitor` 中的空格在 asset 名里被 GitHub 转为点）：
```
https://github.com/Iwantkillbob/wb-monitor/releases/download/v1.1.1/<实际文件名>
```
实际文件名以 `wb-monitor-output\` 里生成的一致为准（见 §4.2）。v1.1.0 当时的链接可作参考：
`https://github.com/Iwantkillbob/wb-monitor/releases/download/v1.1.0/WB.Monitor-1.1.0-win-x64.exe`

---

## 6. 故障排查

| 现象 | 原因 / 处理 |
|---|---|
| 启动后无悬浮球 / 无托盘 | 看项目目录 `debug.log` 与 `boot.log`；确认用 `start.bat` 或 `npm run start:clean`（**别用** `npm start`，缺 GPU 兼容参数会崩） |
| 打包卡死十几分钟或报 `UNKNOWN` | Defender 实时防护锁文件 → 见 §4.1 加排除项（或临时关实时防护） |
| `start.bat` 把 WorkBuddy 也关了 | 旧 footgun 已修；确认 `start.bat` 用的是"按命令行匹配 `*wb-monitor*` 杀进程"，不是 `taskkill /im electron.exe` |
| 打包闪退 / 黑屏 | 无 GPU 环境（VM/远程桌面）需 `start:clean` 的 GPU 参数；打包版已内置 `--in-process-gpu --disable-gpu --no-sandbox --disable-dev-shm-usage` |
| `gh` 报 EOF / 连接失败 | 代理限流 → 用 `env -u HTTPS_PROXY -u HTTP_PROXY gh ...` |
| `git push` 走代理失败 | 用 `git -c http.proxy= -c https.proxy= push` |

---

## 7. 命令速查表

```bat
REM ===== 日常使用 =====
start D:\workbody\2026-07-31-11-14-14\wb-monitor\start.bat   :: 双击等效

REM ===== 打包（先按 §4.1 排除 Defender）=====
cd /d D:\workbody\2026-07-31-11-14-14\wb-monitor
build.bat

REM ===== 发布 =====
git tag v1.1.1
git -c http.proxy= -c https.proxy= push origin v1.1.1
env -u HTTPS_PROXY -u HTTP_PROXY gh release create v1.1.1 --repo Iwantkillbob/wb-monitor --title "WB Monitor v1.1.1" --notes "运行时资源占用优化" "..\wb-monitor-output\WB-Monitor-1.1.1-Portable.exe"
env -u HTTPS_PROXY -u HTTP_PROXY gh release delete v1.1.0 --repo Iwantkillbob/wb-monitor -y
```

---

## 8. 目录结构（速查）

```
wb-monitor/
├── main.js              # 主进程：窗口 / 托盘 / IPC / 数据轮询
├── preload.js           # 渲染桥接
├── index.html / renderer.js / styles.css
├── config.json          # 用户配置
├── start.bat            # Windows 双击启动器（仅杀本程序进程）
├── build.bat            # 打包为 exe（NSIS + Portable）
├── .npmrc               # 国内 npm 镜像
├── package.json         # 版本 1.1.1 + electron-builder 配置
├── modules/
│   ├── tokenSource.js   # WB 本地 JSONL
│   ├── costSource.js    # workbuddy.db 平台计费（mtime 缓存+串行锁）
│   ├── cc-source.js     # CC Switch JSONL（异步扫描）
│   ├── harness-source.js# 本机 harness 自动发现（60s 缓存）
│   ├── network.js / fps.js / token.js
├── scripts/             # start-clean.js / diag.js / verify-boot.js / patch-electron-builder.js
└── 运行手册.md           # 本文档
```

---

## 9. 已知限制
1. CC 费用为按核实单价估算（非官方账单），通常误差 <5%。
2. macOS 网速暂未实现（仅 Windows / Linux）。
3. 首次加载较慢（全量扫描 `~/.claude/projects`），后续 mtime 缓存几乎零开销。
4. v1.1.1 打包发布受本机 Defender 策略阻塞，需管理员加排除项（§4.1）后方可出包。

---

*文档版本 v1.1.1 ｜ 生成于 2026-08-14 ｜ 维护 jarves*
